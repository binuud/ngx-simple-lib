import { MicroTimeDisplayPipe } from './micro-time-display.pipe';

describe('MicroTimeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MicroTimeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
